{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "functions"
  ],
  "scripts": {
    "build:calculator": "npm -w apps/calculator run build",
    "build:admin": "npm -w apps/admin run build",
    "deploy:calculator": "npm run build:calculator && firebase deploy --only hosting:calculator",
    "deploy:admin": "npm run build:admin && firebase deploy --only hosting:admin"
  }
}
